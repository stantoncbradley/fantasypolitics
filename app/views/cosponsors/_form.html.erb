<%= form_for(@cosponsor) do |f| %>
  <% if @cosponsor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cosponsor.errors.count, "error") %> prohibited this cosponsor from being saved:</h2>

      <ul>
      <% @cosponsor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
