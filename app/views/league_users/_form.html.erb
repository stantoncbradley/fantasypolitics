<%= form_for(@league_user) do |f| %>
  <% if @league_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@league_user.errors.count, "error") %> prohibited this league_user from being saved:</h2>

      <ul>
      <% @league_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
